"""Application layer — orchestration.

Use-cases coordinate domain objects and repository ports to fulfill one user action.
They depend on domain abstractions, never on infrastructure or the web framework.
"""
